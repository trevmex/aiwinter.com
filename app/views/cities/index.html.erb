<h1>Listing cities</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
  </tr>

<% @cities.each do |city| %>
  <tr>
    <td><%= city.name %></td>
    <td><%= city.description %></td>
    <td><%= link_to 'Show', city %></td>
  </tr>
<% end %>
</table>

